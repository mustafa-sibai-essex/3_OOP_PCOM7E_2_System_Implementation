from enum import Enum


class EngineState(Enum):
    """Engine state is a class that contains the state of the engine which are on and off"""

    ENGINE_ON = "Engine On"
    """Engine on state"""

    ENGINE_OFF = "Engine Off"
    """Engine off state"""
